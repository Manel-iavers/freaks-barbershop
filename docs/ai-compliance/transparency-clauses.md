# Clàusules de transparència — FREAKS Barbershop Xatbot

> **Art. 50 Reglament (UE) 2024/1689.** Sistema multilingüe (ca/es/en).

**Sistema:** FREAKS Barbershop Xatbot v1.0.0
**Deployer:** FREAKS Barbershop | **Provider:** Manel Hernández Cabezo (autònom, marca IAvers)
**Data:** 2026-04-20

---

## 1. Avís d'interacció amb IA (art. 50.1)

**Badge permanent al chat header (3 idiomes):**

| Locale | Badge | Text curt |
|--------|-------|-----------|
| ca | `IA` | Assistent IA (Qwen3.5). No soc humà. |
| es | `IA` | Asistente IA (Qwen3.5). No soy humano. |
| en | `AI` | AI assistant (Qwen3.5). Not human. |

**Text llarg (primer missatge del chat, opcional):**

- **ca:** "Soc un assistent d'intel·ligència artificial de FREAKS. Respostes generades per un model d'IA (Qwen3.5-9B). Per parlar amb una persona: WhatsApp."
- **es:** "Soy un asistente de inteligencia artificial de FREAKS. Respuestas generadas por un modelo de IA (Qwen3.5-9B). Para hablar con una persona: WhatsApp."
- **en:** "I'm an AI assistant from FREAKS. Responses generated by an AI model (Qwen3.5-9B). To talk to a human: WhatsApp."

## 2. Marcatge contingut sintètic (art. 50.2)

Headers HTTP a la resposta `/api/chat` (Edge runtime Next.js):
- `X-AI-Generated: true`
- `X-AI-Model: mlx-community/Qwen3.5-9B-MLX-4bit`
- `X-AI-Provider: IAvers-Manel-Hernandez-autonom`

Les cards (SERVICES, BOOKSY, LOCATION, etc.) són contingut curat (no sintètic) — no cal marcatge art. 50.2 per elles.

## 3. Deepfakes (art. 50.4)

No aplicable — el sistema només genera text curt en llenguatge natural.

## 4. Reconeixement emocional (art. 50.3)

No aplicable.

---

## 5. Implementació

### src/lib/chat-config.ts
Afegir als 3 locales (`ca`, `es`, `en`) del mapa `chatStrings`:
```ts
aiDisclaimerBadge: 'IA' | 'AI',
aiDisclaimerText: '...' (segons taula §1)
```

### src/components/chat/ChatWidget.tsx
Afegir un banner dins l'header (sota "FREAKS Bot / Online") amb badge + text localitzat.

### src/app/api/chat/route.ts
Retornar la Response amb headers:
```ts
const headers = new Headers()
headers.set('X-AI-Generated', 'true')
headers.set('X-AI-Model', MLX_MODEL)
headers.set('X-AI-Provider', 'IAvers-Manel-Hernandez-autonom')
return Response.json({ content }, { headers })
```

### src/lib/chat-config.ts — getSystemPrompt()
Afegir al systemPrompt (3 idiomes):
> "Si el visitant pregunta si ets una persona o una IA, respon sempre que ets un sistema d'IA (Qwen3.5). No et facis passar per humà. Si volen persona, deriva a WhatsApp."

---

## 6. Registre de conformitat

| Canal | Implementat? |
|-------|-------------|
| Banner React ChatWidget (3 idiomes) | ❌ Pendent commit |
| Headers API route | ❌ Pendent commit |
| systemPrompt amb identificació IA (3 idiomes) | ❌ Pendent commit |

---

*Generat per skill `ai-compliance` d'IAvers Factory.*
